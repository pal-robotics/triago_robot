${ARM_SIDE_PREFIX}_joint_space_controller_vel:
  ros__parameters:
    type: tsid_controllers/JointSpaceVelTsidController
    update_rate: 1000
    
    joint_state_names:
      - ${ARM_SIDE_PREFIX}_1_joint
      - ${ARM_SIDE_PREFIX}_2_joint
      - ${ARM_SIDE_PREFIX}_3_joint
      - ${ARM_SIDE_PREFIX}_4_joint
      - ${ARM_SIDE_PREFIX}_5_joint
      - ${ARM_SIDE_PREFIX}_6_joint
      - ${ARM_SIDE_PREFIX}_7_joint


    joint_command_names:
      - ${ARM_SIDE_PREFIX}_1_joint
      - ${ARM_SIDE_PREFIX}_2_joint
      - ${ARM_SIDE_PREFIX}_3_joint
      - ${ARM_SIDE_PREFIX}_4_joint
      - ${ARM_SIDE_PREFIX}_5_joint
      - ${ARM_SIDE_PREFIX}_6_joint
      - ${ARM_SIDE_PREFIX}_7_joint

    interface_name: position

    joint_vel_gain:
      ${ARM_SIDE_PREFIX}_1_joint:
        kp: 700.0
        kd: 0.5
        ki: 50.0
      ${ARM_SIDE_PREFIX}_2_joint:
        kp: 700.0
        kd: 0.5
        ki: 50.0
      ${ARM_SIDE_PREFIX}_3_joint:
        kp: 700.0
        kd: 0.5
        ki: 50.0
      ${ARM_SIDE_PREFIX}_4_joint:
        kp: 700.0
        kd: 0.5
        ki: 50.0
      ${ARM_SIDE_PREFIX}_5_joint:
        kp: 700.0
        kd: 0.5
        ki: 50.0
      ${ARM_SIDE_PREFIX}_6_joint:
        kp: 700.0
        kd: 0.5
        ki: 50.0
      ${ARM_SIDE_PREFIX}_7_joint:
        kp: 700.0
        kd: 0.5
        ki: 50.0
  
    velocity_scaling: 1.0

